/**
const tap = require('tap');
const Bridge = require('../../../lib/bridge');

tap.test('Bridge', (l0) => {
    const bridgeA = new Bridge({config: {id: 'bridgeA', request: {waitTime: 2000}}});
    const bridgeB = new Bridge({config: {id: 'bridgeB', request: {waitTime: 2000}}});
    l0.test('??????', (l1) => {
        bridge.destroy();
        l1.end();
    });

    l0.end();
});

*/