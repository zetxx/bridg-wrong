language: node_js
node_js:
- "9"

script: echo "Running tests against $(node -v) ..."

jobs:
  include:
    - stage: test
      script: npm run unit-test
    - stage: release
      deploy:
        only:
          - master
        provider: script
        skip_cleanup: true
        script:
          - npx semantic-release