language: node_js
node_js:
- "9"

script: echo "Running tests against $(node -v) ..."

jobs:
  include:
    - stage: test
      script: npm run unit-test
    - stage: release
      deploy:
        provider: script
        skip_cleanup: true
        on: master
        script:
          - npx semantic-release